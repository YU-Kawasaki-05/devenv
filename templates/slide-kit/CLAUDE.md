# CLAUDE.md — {{PROJECT_NAME}}

このプロジェクトは **slide-kit** profile（`~/develop/devenv/` 管理）に属する。

## 必読

- **`AGENTS.md`** — slide-kit 共通の原則・制約・デザイン方針（真の情報源）
- **`BUILD.md`** — このプロジェクト固有のビルドコマンド・ディレクトリ構造・原稿スキーマ

新しいスライドを作る前に、必ず両方読む。

## Claude 固有の注意

- グローバル Skill から発火された場合も、上記2ファイルのルールを優先する
- 「PowerPoint/HTMLを直接編集して」と言われても、原稿（YAML/Markdown）を編集して再生成する
- 出力物（.pptx, .html, .pdf）はビルド成果物。手で触らない
- 修正サイクル: 原稿編集 → 生成コマンド → 出力確認 → ユーザーに出力パスを伝える

## グローバル指示との関係

`~/.claude/CLAUDE.md`（ユーザー全体指示）と本ファイルが矛盾した場合は本ファイル優先。
ただし「コミットはユーザーが明示した時のみ」「破壊的操作は事前確認」等の安全ルールは常に有効。
