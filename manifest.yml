# devenv project → profile mapping.
# scripts/sync.sh and scripts/diff.sh resolve the profile from this file
# (key = basename of the project directory).
#
# Add a project here once it has been migrated to a managed profile.

projects:
  Ardors-website: docs-first-web
  schedule_app: docs-first-web
  lifeapp: docs-first-web         # uses overrides/lifeapp/ for project-specific files
  # jovin: pending — currently empty CLAUDE.md, decide profile after content review.
