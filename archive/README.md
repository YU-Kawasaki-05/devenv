# archive/

過去に使っていたが置き換えられた成果物の保管庫。**参照のみ。新規作業では使わない**。

各サブディレクトリは「{元の場所}_{バージョン}_{アーカイブ日}」形式で命名し、
中に元コンテンツと、置き換え経緯を記した `_about.md` を置く。

## 現在のアーカイブ

| サブディレクトリ | 元の場所 | 置き換え理由 |
|---|---|---|
| `要件定義_v1_2026-05-09/` | `~/.claude/commands/要件定義/` | v2（drift検出・per-FR・README/drawio生成等を追加した再設計）に置き換え |
